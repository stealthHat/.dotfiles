#!/bin/sh

#my alias
alias \
  fucking='sudo' \
  please='sudo' \
  sshconfig='vim ~/.ssh/config' \
  sshkey='cat ~/.ssh/my_key.pub' \
  zshrc='nvim $HOME/git/.dotfiles/.config/zsh/.zshrc' \
  zshaliases='nvim $HOME/git/.dotfiles/.config/zsh/.zsh_aliases' \
  l='ls -lha' \
  zshupdate='source $HOME/.config/zsh/.zshrc' \
  s='git status' \
  k='kubectl' \
  vim='nvim' \
  kp='kubectl get pods' \
  r='rancher' \
  rk='rancher kubectl' \
  t='terraform' \
  diff='diff --color=auto' \
  pacman='sudo pacman'
