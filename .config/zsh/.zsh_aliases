#!/bin/sh

#my alias
alias \
  fucking='sudo' \
  please='sudo' \
  sshconfig='vim ~/.ssh/config' \
  sshkey='cat ~/.ssh/my_key.pub' \
  zshaliases='nvim $HOME/.config/zsh/.zsh_aliases' \
  zshrc='nvim $HOME/.config/zsh/.zshrc' \
  l='ls -la --color=auto --group-directories-first' \
  zshupdate='source $HOME/.config/zsh/.zshrc' \
  git s='git status' \
  k='kubectl' \
  vim='nvim' \
  kp='kubectl get pods' \
  r='rancher' \
  rk='rancher kubectl' \
  e='eksctl' \
  t='terraform' \
  grep='grep --color=auto' \
  diff='diff --color=auto' \
  pacman='sudo pacman'

#kitty ssh alias
alias ssh='kitty +kitten ssh'

#ssh alias
alias strada='ssh strada'
alias cargo='ssh cargo'
alias fiorino='ssh fiorino'
alias kangoo='ssh kangoo'
alias pampa='ssh pampa'
alias saveiro='ssh saveiro'
alias sprinter='ssh sprinter'
alias soul='ssh soul'
alias saveirohom='ssh saveirohom'
alias stradahom='ssh stradahom'
alias cig='ssh cig7'
