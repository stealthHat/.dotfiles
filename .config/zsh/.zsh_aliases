#!/bin/sh

#my alias
alias \
  pacman='sudo pacman' \
  fucking='sudo' \
  please='sudo' \
  sshconfig='vim $HOME/.ssh/config' \
  sshkey='cat $HOME/.ssh/my_key.pub' \
  zshrc='nvim $HOME/git/.dotfiles/.config/zsh/.zshrc' \
  zshaliases='nvim $HOME/git/.dotfiles/.config/zsh/.zsh_aliases' \
  l='ls -lha' \
  zshupdate='source $HOME/.config/zsh/.zshrc' \
  s='git status' \
  k='kubectl' \
  vim='nvim' \
  kp='kubectl get pods' \
  kg='kubectl -n gitlab-ci-interno' \
  ks='kubectl -n kube-system' \ 
  r='rancher' \
  rk='rancher kubectl' \
  t='terraform' \
  diff='diff --color=auto' \
  pacman='sudo pacman' \
  kd='kubectl -n devops-apps' \
