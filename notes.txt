1 - Run setup.sh
  
2 - zsh
  $ curl -sfL git.io/antibody | sh -s - -b /usr/local/bin
  $ sudo pacman -S zsh
  $ sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" # Install oh-my-zsh
  $ chsh -$(whichzsh) # Seta o zsh como shell padrao caso o oh-my-zsh não conseguir
 
3 - Nvim:
  $ sudo pacman -Syuu install neovim
  $ curl -fLo ~/.local/share/nvim/site/autoload/plug.vim --create-dir\
       https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
  ? Inside vim :PlugInstall

4 - Tmux
  ? Install tmux via package manager 
  $ git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm # Install tmux plugin manager 
  ? Inside tmux: leader + I 












next steps: 
install newsbeuter
install neomutt
